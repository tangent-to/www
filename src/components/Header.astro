---
import Container from "@components/Container.astro";
import Link from "@components/Link.astro";
import LanguageSwitcher from "@components/LanguageSwitcher.astro";
import { SITE } from "@consts";
import { t } from "@lib/i18n";

// Detect language
const currentPath = Astro.url.pathname;
const currentLang = currentPath.startsWith('/fr') ? 'fr' : 'en';
const langPrefix = currentLang === 'fr' ? '/fr' : '';
const homeHref = langPrefix === '' ? '/' : langPrefix;
---

<header>
  <Container>
    <div class="flex flex-wrap gap-y-2 items-center justify-between">
      <Link href={homeHref} underline={false} class="flex items-center">
        <img 
          src="/logo-light.svg" 
          alt={SITE.NAME}
          class="h-8 dark:hidden whitespace-nowrap"
        />
        <img 
          src="/logo-dark.svg" 
          alt={SITE.NAME}
          class="hidden h-8 dark:block whitespace-nowrap"
        />
      </Link>
      <div class="flex items-center gap-3">
        <nav class="flex gap-1">
          <Link href={`${langPrefix}/projects`}>
            {t("nav.projects", currentLang)}
          </Link>
          <span>
            {`/`}
          </span>
          <Link href={`${langPrefix}/about`}>
            {t("nav.about", currentLang)}
          </Link>
          <span>
            {`/`}
          </span>
          <Link href={`${langPrefix}/contact`}>
            {t("nav.contact", currentLang)}
          </Link>
        </nav>
        <LanguageSwitcher />
      </div>
    </div>
  </Container>
</header>
